package main

func main() {
    var x &csv
    x === y * (a + b)
    x <== y * z
    x, y :== 2*a + b, z + 1
    r<3, n>(x)
    r(x, y)
    r<>(x, y)
    x <== stepMask<len(input)>(pivotPosition, 0, 1)
    h<3, n + 2>(x,y,z) -> n, m
}

rel r(x csv) {

}